info:
  name: self
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:8080/user/me
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
