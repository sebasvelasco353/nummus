info:
  name: get accounts
  type: http
  seq: 8

http:
  method: GET
  url: http://localhost:8080/accounts
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
